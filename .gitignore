#
# Documentation is here: http://git-scm.com/docs/gitignore
#
tmp/
unpack/

#
# We are now using collectstatic to copy static files to this
# directory for serving outside of wsgi.
# So we do not want to check these generated copies into git.
#
Site/static/admin
Site/static/content

#
# Trying something a little different with the django settings -
# using a hidden placeholder file so we do not have to mkdir so much.
#
gitignored/Site/Site/RCS
gitignored/Site/Site/settings.py

# I like to switch between netbeans, eclipse, and vim
#
/nbproject/
/.project
__pycache__/

#
# For vim temporary files
#
*.swp
.~lock.*
.*~
*~

# ###############################################################
#
# Following is a copy of the django/.gitignore file
# I am assuming we will want to ignore all these files as well?
#
# ###############################################################
# If you need to exclude files such as those generated by an IDE, use
# $GIT_DIR/info/exclude or the core.excludesFile configuration variable as
# described in https://git-scm.com/docs/gitignore

*.egg-info
*.pot
*.py[co]
.tox/
__pycache__
MANIFEST
dist/
docs/_build/
docs/locale/
node_modules/
tests/coverage_html/
tests/.coverage
build/
tests/report/
